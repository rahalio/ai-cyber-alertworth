/**
 * ExecuteListTriageShifts - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: triage
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListTriageShifts Use Case
 *
 * DDD: Application use case for list Shift.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListTriageShiftsInput, ListTriageShiftsOutput } from "../dto/shift.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alertworth/services/_shared/index.js";
import type { ShiftRepository } from "../ports";

export class ExecuteListTriageShifts {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly shift: ShiftRepository
  ) {}

  async execute(input: ListTriageShiftsInput): Promise<ListTriageShiftsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.trgId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.shift.listTriageShifts({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListTriageShiftsOutput;
  }
}
