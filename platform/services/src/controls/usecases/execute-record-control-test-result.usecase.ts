/**
 * ExecuteRecordControlTestResult - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: controls
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecordControlTestResult Use Case
 *
 * DDD: Application use case for get ControlTestResult.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecordControlTestResultInput, RecordControlTestResultOutput } from "../dto/control-test-result.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alertworth/services/_shared/index.js";
import type { ControlTestResultRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRecordControlTestResult {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly controlTestResult: ControlTestResultRepository
  ) {}

  async execute(input: RecordControlTestResultInput): Promise<RecordControlTestResultOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctlId();

    // All field names extracted from OpenAPI spec: detail, evidenceRef, outcome
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).testId) {
      throw new ValidationError("testId is required");
    }
    const testId = (input as any).testId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.controlTestResult.recordControlTestResult({ testId: testId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${testId}`);
    }

    return entity as RecordControlTestResultOutput;
  }
}
