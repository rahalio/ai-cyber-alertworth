/**
 * Baselines DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: baselines
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Baselines Domain Module - Composition root
 *
 * DDD: Composition root for baselines domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ActivateRepositoryAdapter,
  AnomalyRepositoryAdapter,
  AttackPathRepositoryAdapter,
  BaselineRepositoryAdapter,
  DecisionRepositoryAdapter,
} from "@alertworth/adapters/baselines";
import { getIdGeneratorService } from "@alertworth/adapters";
import type { AdapterDynamoDBClient } from "@alertworth/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateBehaviourBaseline,
  ExecuteListAnomalyDetections,
  ExecuteListAttackPaths,
  ExecuteListBehaviourBaselines,
} from "@alertworth/services/baselines/usecases";
// Ports (interfaces)
import type {
  ActivateRepository,
  AnomalyRepository,
  AttackPathRepository,
  BaselineRepository,
  DecisionRepository,
} from "@alertworth/services/baselines/ports";

export interface BaselinesDomainModule {
  repos: {
    activates: ActivateRepository;
    anomalies: AnomalyRepository;
    attackPaths: AttackPathRepository;
    baselines: BaselineRepository;
    decisions: DecisionRepository;
  };
  useCases: {
    anomalies: {
      list: ExecuteListAnomalyDetections;
    };
    attackPaths: {
      list: ExecuteListAttackPaths;
    };
    baselines: {
      create: ExecuteCreateBehaviourBaseline;
      list: ExecuteListBehaviourBaselines;
    };
  };
}

export function buildBaselinesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): BaselinesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      activates?: ActivateRepository;
      anomalies?: AnomalyRepository;
      attackPaths?: AttackPathRepository;
      baselines?: BaselineRepository;
      decisions?: DecisionRepository;
    } = {};

    obj.activates = new ActivateRepositoryAdapter(dynamoClient);
    obj.anomalies = new AnomalyRepositoryAdapter(dynamoClient);
    obj.attackPaths = new AttackPathRepositoryAdapter(dynamoClient);
    obj.baselines = new BaselineRepositoryAdapter(dynamoClient);
    obj.decisions = new DecisionRepositoryAdapter(dynamoClient);
    return {
      activates: obj.activates!,
      anomalies: obj.anomalies!,
      attackPaths: obj.attackPaths!,
      baselines: obj.baselines!,
      decisions: obj.decisions!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    anomalies: {
      list: new ExecuteListAnomalyDetections(executionContext, idGenerator, repos.anomalies),
    },
    attackPaths: {
      list: new ExecuteListAttackPaths(executionContext, idGenerator, repos.attackPaths),
    },
    baselines: {
      create: new ExecuteCreateBehaviourBaseline(executionContext, idGenerator, repos.baselines),
      list: new ExecuteListBehaviourBaselines(executionContext, idGenerator, repos.baselines),
    },
  };
  return { repos, useCases: useCases as BaselinesDomainModule["useCases"] };
}
