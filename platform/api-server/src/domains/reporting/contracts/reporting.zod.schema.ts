/**
 * Reporting Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reporting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const publishResidualRiskSnapshot_Body = z
  .object({
    asOf: z.string().datetime({ offset: true }),
    audience: z.enum(['operations', 'risk_committee', 'board']),
  })
  .partial()
  .passthrough();
const updateCyberRiskAppetite_Body = z
  .object({
    id: z.string().regex(/^cra_[0-9A-HJKMNP-TV-Z]{26}$/),
    version: z.string(),
    narrative: z.string(),
    thresholds: z
      .array(
        z
          .object({ name: z.string(), limit: z.number() })
          .partial()
          .passthrough()
      )
      .optional(),
    approvedBy: z.string().optional(),
    approvedAt: z.string().datetime({ offset: true }).optional(),
    createdAt: z.string().datetime({ offset: true }),
    updatedAt: z.string().datetime({ offset: true }),
  })
  .passthrough();
const assembleCommitteePack_Body = z
  .object({
    snapshotId: z.string().regex(/^rrs_[0-9A-HJKMNP-TV-Z]{26}$/),
    committee: z.enum(['audit_risk', 'board', 'cyber_steering']).optional(),
  })
  .passthrough();

export const schemas: any = {
  publishResidualRiskSnapshot_Body,
  updateCyberRiskAppetite_Body,
  assembleCommitteePack_Body,
};

const endpoints = makeApi([
  {
    method: 'post',
    path: '/v1/committee-packs',
    alias: 'assembleCommitteePack',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: assembleCommitteePack_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^pck_[0-9A-HJKMNP-TV-Z]{26}$/),
            snapshotId: z.string().regex(/^rrs_[0-9A-HJKMNP-TV-Z]{26}$/),
            committee: z.string(),
            packUri: z.string().url().optional(),
            assembledAt: z.string().datetime({ offset: true }).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/cyber-risk-appetite',
    alias: 'getCyberRiskAppetite',
    requestFormat: 'json',
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^cra_[0-9A-HJKMNP-TV-Z]{26}$/),
            version: z.string(),
            narrative: z.string(),
            thresholds: z
              .array(
                z
                  .object({ name: z.string(), limit: z.number() })
                  .partial()
                  .passthrough()
              )
              .optional(),
            approvedBy: z.string().optional(),
            approvedAt: z.string().datetime({ offset: true }).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'put',
    path: '/v1/cyber-risk-appetite',
    alias: 'updateCyberRiskAppetite',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: updateCyberRiskAppetite_Body,
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^cra_[0-9A-HJKMNP-TV-Z]{26}$/),
            version: z.string(),
            narrative: z.string(),
            thresholds: z
              .array(
                z
                  .object({ name: z.string(), limit: z.number() })
                  .partial()
                  .passthrough()
              )
              .optional(),
            approvedBy: z.string().optional(),
            approvedAt: z.string().datetime({ offset: true }).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/residual-risk/snapshots',
    alias: 'listResidualRiskSnapshots',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  id: z.string().regex(/^rrs_[0-9A-HJKMNP-TV-Z]{26}$/),
                  asOf: z.string().datetime({ offset: true }).optional(),
                  residualLevel: z.enum(['low', 'medium', 'high', 'critical']),
                  againstAppetite: z.enum(['within', 'near', 'breached']),
                  staleKriCount: z.number().int().optional(),
                  openHighWorthAlerts: z.number().int().optional(),
                  audience: z
                    .enum(['operations', 'risk_committee', 'board'])
                    .optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/residual-risk/snapshots',
    alias: 'publishResidualRiskSnapshot',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: publishResidualRiskSnapshot_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^rrs_[0-9A-HJKMNP-TV-Z]{26}$/),
            asOf: z.string().datetime({ offset: true }).optional(),
            residualLevel: z.enum(['low', 'medium', 'high', 'critical']),
            againstAppetite: z.enum(['within', 'near', 'breached']),
            staleKriCount: z.number().int().optional(),
            openHighWorthAlerts: z.number().int().optional(),
            audience: z
              .enum(['operations', 'risk_committee', 'board'])
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
