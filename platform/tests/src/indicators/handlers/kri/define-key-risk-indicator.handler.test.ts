/**
 * DefineKeyRiskIndicator handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: indicators
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { defineKeyRiskIndicator } from "@alertworth/api-server/domains/indicators/handlers";
import type { IndicatorsDDDDependencies } from "@alertworth/api-server/domains/indicators";
import { createKri as createKriEntity } from "@alertworth/factories/indicators";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alertworth/tests/shared";
import { INDICATORS_USE_CASES } from "../__factories__/use-cases.js";

describe("DefineKeyRiskIndicator", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IndicatorsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IndicatorsDDDDependencies>(
      INDICATORS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createKriEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeDefineKeyRiskIndicator, "execute").mockResolvedValue(useCaseResult);

    await defineKeyRiskIndicator(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeDefineKeyRiskIndicator.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
