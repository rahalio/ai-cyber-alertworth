/**
 * PublishResidualRiskSnapshot handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reporting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { publishResidualRiskSnapshot } from "@alertworth/api-server/domains/reporting/handlers";
import type { ReportingDDDDependencies } from "@alertworth/api-server/domains/reporting";
import { createSnapshot as createSnapshotEntity } from "@alertworth/factories/reporting";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alertworth/tests/shared";
import { REPORTING_USE_CASES } from "../__factories__/use-cases.js";

describe("PublishResidualRiskSnapshot", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReportingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReportingDDDDependencies>(
      REPORTING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createSnapshotEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executePublishResidualRiskSnapshot, "execute").mockResolvedValue(useCaseResult);

    await publishResidualRiskSnapshot(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executePublishResidualRiskSnapshot.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
