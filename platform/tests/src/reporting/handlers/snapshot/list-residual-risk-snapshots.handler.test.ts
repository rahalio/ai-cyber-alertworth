/**
 * ListResidualRiskSnapshots handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reporting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listResidualRiskSnapshots } from "@alertworth/api-server/domains/reporting/handlers";
import type { ReportingDDDDependencies } from "@alertworth/api-server/domains/reporting";
import { createSnapshot as createSnapshotEntity } from "@alertworth/factories/reporting";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alertworth/tests/shared";
import { REPORTING_USE_CASES } from "../__factories__/use-cases.js";

describe("ListResidualRiskSnapshots", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReportingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReportingDDDDependencies>(
      REPORTING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createSnapshotEntity({ orgId }),
      createSnapshotEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListResidualRiskSnapshots, "execute").mockResolvedValue(useCaseResult);

    await listResidualRiskSnapshots(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListResidualRiskSnapshots.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
