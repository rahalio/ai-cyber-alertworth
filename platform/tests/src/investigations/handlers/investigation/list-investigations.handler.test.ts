/**
 * ListInvestigations handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: investigations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listInvestigations } from "@alertworth/api-server/domains/investigations/handlers";
import type { InvestigationsDDDDependencies } from "@alertworth/api-server/domains/investigations";
import { createInvestigation as createInvestigationEntity } from "@alertworth/factories/investigations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alertworth/tests/shared";
import { INVESTIGATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListInvestigations", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: InvestigationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<InvestigationsDDDDependencies>(
      INVESTIGATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createInvestigationEntity({ orgId }),
      createInvestigationEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListInvestigations, "execute").mockResolvedValue(useCaseResult);

    await listInvestigations(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListInvestigations.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
