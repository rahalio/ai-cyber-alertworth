# @alertworth/webapp

Alertworth detection-economics SOC console (Next.js 14 App Router).

- Brand tokens and IA: repo-root `WEBAPP.md`
- Domain API clients / feature scaffolds: filled by zero-codegen under `src/services/domains` and `src/features/{domain}`
- Handwritten priority screens: queue, overflow, investigations, policy, KRIs

```bash
pnpm --filter @alertworth/webapp dev
```
